{
  "name": "minimal-mongo-server",
  "version": "1.0.0",
  "description": "a server to connect to mongodb and return data",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "commander": "^8.3.0",
    "faker": "^5.5.3",
    "node-fetch": "^2.6.6",
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "mongodb": "^4.2.0",
    "path-to-regexp": "^6.2.0"
  },
  "nodemonConfig": {
    "ignore": [
      "scripts",
      "README.md"
    ]
  }
}
